import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.obilocus.picnoh',
  appName: 'Picnoh',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
